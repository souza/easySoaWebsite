#set($content = $serviceManager.searchFile($file))
$request.getSession().setAttribute("file",$content)
$request.getSession().setAttribute("fileName",$file)
#if($file.endsWith("java"))
    $request.getSession().setAttribute("extension","java")
#end
#if($file.endsWith("xml"))
    $request.getSession().setAttribute("extension","xml")
#end
#if($file.endsWith("composite"))
    $request.getSession().setAttribute("extension","composite")
#end
$response.sendRedirect($request.getHeader("Referer"))
